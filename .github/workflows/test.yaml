name: "test"

on:
  pull_request:
    branches:
      - "master"
  push:
    branches:
      - "master"

jobs:
  test-auto:
    name: "Test"
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v2
      - name: Run
        run: echo test
